/*** Like I said, really easy ***/

public boolean checkResponseCode(String url) {

	int status = webClient.getPage(url).getWebResponse().getStatusCode();
	
	if ( (status >= 200) && (status < 300) ) {
		return true;
	} else {
		return false;
	}
}